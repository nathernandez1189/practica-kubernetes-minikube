apiVersion: v1
kind: Service
metadata:
  name: hello-node
  labels:
    app: hello-node
spec:
  type: NodePort
  selector:
    app: hello-node
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
